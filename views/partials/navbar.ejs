<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/dashboard">Odoo Expenses</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if (user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                    
                    <% if (user.role === 'Employee') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/expenses/submit">Submit Expense</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/expenses/history">My History</a>
                        </li>
                    <% } %>

                    <% if (user.role === 'Manager' || user.role === 'Admin') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/expenses/pending">Pending Approvals</a>
                        </li>
                    <% } %>
                    
                    <% if (user.role === 'Admin') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/users/new">Manage Users</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/rules/config">Config Rules</a>
                        </li>
                    <% } %>
                <% } %>
            </ul>
            
            <ul class="navbar-nav">
                <% if (user) { %>
                    <li class="nav-item">
                        <span class="navbar-text me-3">
                            Logged in as: <strong><%= user.username %></strong> (<%= user.role %>)
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-danger" href="/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="btn btn-outline-light me-2" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-warning" href="/signup">Signup</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>